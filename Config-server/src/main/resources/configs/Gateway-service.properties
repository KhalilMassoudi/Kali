server.port=8080
jwt.secret=pPFM3kM5_3idcnhixl8AcKNtfhbmiUurkwXzR4GNJIvIIaXctfq7fyJjS5_nLOvcU6_pXrtHeQiyIJO1LamLMA==

# The Gateway forwards raw request bytes as-is (see GatewayRoutesConfig.proxyTo) — it must
# never let Spring's DispatcherServlet parse multipart bodies itself (e.g. ticket attachment
# uploads), or the raw bytes it re-forwards to the target service would be corrupted/emptied.
spring.servlet.multipart.enabled=false